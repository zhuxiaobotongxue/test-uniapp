import {
  httpRequest
} from '@/utils'

/**
 * @function 账号密码登录
 */
export const loginByPwd = () => {
  return new Promise((resolve, reject) => {
    resolve({
      code: 200,
      data: null
    })
  })
}
